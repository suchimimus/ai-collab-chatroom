version: "3.9"

services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  ws-server:
    build: ./ws-server
    ports:
      - "8080:8080"
    depends_on:
      - redis
    environment:
      REDIS_URL: redis://redis:6379
